version: '3.8'
services:
  backend:
    build: ../api
    ports:
      - "5000:5000"
    volumes:
      - ../models:/models
      - ../data:/data
  frontend:
    build: ../frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
